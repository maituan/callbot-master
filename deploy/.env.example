# Copy to deploy/.env and fill in values for `docker compose -f deploy/docker-compose.yml up`.
# Only variables you want to override need to be set; everything else falls
# back to the defaults baked into docker-compose.yml.

# --- Provider endpoints --------------------------------------------------
# MASTER_BOT_URL=http://160.250.216.28:11005/api/v1/call/
# MASTER_ASR_ENDPOINT=103.253.20.28:9000
# MASTER_ASR_TOKEN=
# MASTER_TTS_ENDPOINT=ws://103.253.20.27:8767
# MASTER_TTS_TOKEN=
# MASTER_TTS_VOICE_ID=thuyanh-north

# --- FreeSWITCH ----------------------------------------------------------
# MASTER_FS_HOST=101.99.3.216:8021
# MASTER_FS_PASSWORD=ClueCon
# MASTER_FS_DOMAIN=voiceai.autotele.vn
# MASTER_INBOUND_DID=5000000024

# --- Pipeline knobs ------------------------------------------------------
# MASTER_BARGE_IN=true
# MASTER_LOG_LEVEL=debug

# --- Persistence (override to point at an external DB) -------------------
# MASTER_PG_DSN=postgres://user:pass@db.example:5432/callbot?sslmode=require

# --- Telemetry -----------------------------------------------------------
# OTEL_EXPORTER_OTLP_ENDPOINT=tempo:4317
# MASTER_OTEL_INSECURE=true
